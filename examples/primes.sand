{
    let Num.divisible = (n) {
        self.mod(n).eq(0);
    };

    let Num.is_prime = () {
        [2..self]
            .find(self.divisible)
            .eq(false)
    };

    100.times((n) {
        n.is_prime().then(() {
            print(n.to_str())
        })
    });
}
